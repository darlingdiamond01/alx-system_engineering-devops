#!/usr/bin/env ruby
# This script has the regular expression that will match the baove cases that accepts one argument and pass it to a regular expression matching method and should not contain square brackets
puts ARGV[0].scan(/hbt*n/).join
